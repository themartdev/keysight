package dev.simonmartineau.keysight.score

enum class Clef { TREBLE, BASS }

/** Which hand a note belongs to. V1 is right hand only; see docs/implementation-plan.md. */
enum class Hand { LEFT, RIGHT }
