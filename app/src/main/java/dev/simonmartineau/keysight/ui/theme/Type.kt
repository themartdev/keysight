package dev.simonmartineau.keysight.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 defaults for now. Practice UI is a handful of numbers and one large countdown, so
 * the type scale only needs revisiting when the result screen gets its real design.
 */
internal val KeySightTypography = Typography()
